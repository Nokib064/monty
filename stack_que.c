#include "monty.h"
/**
 * _stack - prints the top
 * @head: stack head
 * @num_line: line_number
 * Return: nothing
 */
void _stack(stack_t **head, unsigned int num_line)
{
	(void)head;
	(void)num_line;
	valu.stack_que = 0;
}


#include "monty.h"
/**
 * _queue - prints the top
 * @head: stack head
 * @num_line: line_number
 * Return: nothing
 */
void _queue(stack_t **head, unsigned int num_line)
{
	(void)head;
	(void)num_line;
	valu.stack_que = 1;
}
